import { FormEvent, useEffect, useState } from 'react';
import { Plus } from 'lucide-react';
import { useApp } from '../contexts/AppContext'; import { supabase } from '../lib/supabase'; import { Counterparty } from '../lib/types';
import { Button } from '../components/Button'; import { Input } from '../components/Input'; import { Modal } from '../components/Modal';
export function Suppliers(){const{workspaceId}=useApp();const[rows,setRows]=useState<Counterparty[]>([]);const[open,setOpen]=useState(false);const[form,setForm]=useState({name:'',email:'',vat_trn:''});const[msg,setMsg]=useState('');
const load=async()=>{if(!workspaceId)return;const{data,error}=await supabase.from('counterparties').select('*').eq('workspace_id',workspaceId).or('kind.eq.vendor,kind.eq.both').order('name');if(error)setMsg(error.message);else setRows((data||[])as Counterparty[])};useEffect(()=>{void load()},[workspaceId]);
const save=async(e:FormEvent)=>{e.preventDefault();if(!workspaceId)return;const{error}=await supabase.from('counterparties').insert({workspace_id:workspaceId,name:form.name,company_name:form.name,email:form.email||null,vat_trn:form.vat_trn||null,kind:'vendor'});if(error)return setMsg(error.message);setOpen(false);setForm({name:'',email:'',vat_trn:''});void load()};
return <div><div className="flex justify-between mb-6"><div><h1 className="text-3xl font-bold">Suppliers</h1><p className="text-gray-600 mt-1">External invoices, cost of revenue and operating expenses.</p></div><Button onClick={()=>setOpen(true)}><Plus className="w-4 h-4 mr-2"/>Add supplier</Button></div>{msg&&<div className="mb-4 bg-amber-50 p-3 rounded">{msg}</div>}<div className="bg-white border rounded-xl overflow-hidden"><table className="w-full"><thead className="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th className="p-4">Supplier</th><th>Email</th><th>VAT / tax number</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t"><td className="p-4 font-medium">{r.company_name||r.name}</td><td>{r.email||'—'}</td><td>{r.vat_trn||'—'}</td></tr>)}{!rows.length&&<tr><td colSpan={3} className="p-10 text-center text-gray-500">No suppliers yet.</td></tr>}</tbody></table></div><Modal isOpen={open} onClose={()=>setOpen(false)} title="Add supplier"><form onSubmit={save} className="space-y-4"><Input required label="Registered supplier name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><Input type="email" label="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><Input label="VAT / tax number" value={form.vat_trn} onChange={e=>setForm({...form,vat_trn:e.target.value})}/><div className="flex justify-end gap-2"><Button type="button" variant="secondary" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit">Save supplier</Button></div></form></Modal></div>}
