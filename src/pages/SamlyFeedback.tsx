import { FormEvent, useEffect, useState } from 'react';
import { supabase } from '../lib/supabase';
import { Button } from '../components/Button';
import { Input } from '../components/Input';
import { Select } from '../components/Select';
import { useLanguage } from '../contexts/LanguageContext';

type Request = { id: string; title: string; description: string; category: string; status: string; admin_note: string | null; created_at: string };
function navigate(path: string) { window.history.pushState({}, '', path); window.dispatchEvent(new PopStateEvent('popstate')); }

export function SamlyFeedback() {
  const { language } = useLanguage(); const de = language === 'de';
  const [signedIn, setSignedIn] = useState(false); const [admin, setAdmin] = useState(false); const [rows, setRows] = useState<Request[]>([]); const [title, setTitle] = useState(''); const [description, setDescription] = useState(''); const [category, setCategory] = useState('idea'); const [message, setMessage] = useState(''); const [busy, setBusy] = useState(false);
  const load = async () => { const { data: { user } } = await supabase.auth.getUser(); setSignedIn(!!user); if (!user) return; const [{ data: isAdmin }, { data, error }] = await Promise.all([supabase.rpc('is_samly_admin'), supabase.from('samly_feature_requests').select('id,title,description,category,status,admin_note,created_at').order('created_at', { ascending: false })]); setAdmin(!!isAdmin); if (error) setMessage(error.message); else setRows((data || []) as Request[]); };
  useEffect(() => { void load(); }, []);
  const submit = async (event: FormEvent) => { event.preventDefault(); setBusy(true); setMessage(''); const { error } = await supabase.rpc('submit_samly_feature_request', { p_title: title, p_description: description, p_category: category }); setBusy(false); if (error) { setMessage(error.message); return; } setTitle(''); setDescription(''); setMessage(de ? 'Danke — dein Vorschlag ist bei Onkel Samly angekommen.' : 'Thanks — Uncle Samly has received your idea.'); await load(); };
  const review = async (id: string, status: string) => { const { error } = await supabase.rpc('review_samly_feature_request', { p_request_id: id, p_status: status, p_admin_note: null }); if (error) setMessage(error.message); else await load(); };
  return <main className="min-h-screen bg-[#fbfaf8] px-5 py-8 text-slate-900 sm:px-8"><div className="mx-auto max-w-4xl"><button onClick={() => navigate('/samly')} className="text-xl font-bold tracking-tight">samly<span className="text-blue-600">.</span></button><section className="mt-10 grid gap-8 lg:grid-cols-[.85fr_1.15fr]"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-blue-700">{de ? 'WÜNSCH DIR WAS' : 'MAKE A WISH'}</p><h1 className="mt-3 text-4xl font-bold tracking-tight">{de ? 'Was soll Onkel Samly als Nächstes erledigen?' : 'What should Uncle Samly tackle next?'}</h1><p className="mt-4 leading-7 text-slate-600">{de ? 'Gute Ideen kommen nicht in die Schublade. Sie kommen in den Samly-Inbox.' : 'Good ideas do not disappear into a drawer. They go into the Samly inbox.'}</p>{!signedIn && <div className="mt-6 rounded-2xl border border-blue-200 bg-blue-50 p-4 text-sm text-blue-900">{de ? 'Bitte melde dich an, um einen Vorschlag einzureichen.' : 'Please sign in to submit a feature request.'} <button onClick={() => navigate('/samly/login')} className="font-semibold underline">{de ? 'Anmelden' : 'Sign in'}</button></div>}</div><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><form onSubmit={submit} className="space-y-4"><Input label={de ? 'Kurzer Titel' : 'Short title'} value={title} onChange={event => setTitle(event.target.value)} required disabled={!signedIn} /><Select label={de ? 'Art' : 'Type'} value={category} onChange={event => setCategory(event.target.value)} disabled={!signedIn}><option value="idea">{de ? 'Neue Idee' : 'New idea'}</option><option value="improvement">{de ? 'Verbesserung' : 'Improvement'}</option><option value="bug">{de ? 'Etwas funktioniert nicht' : 'Something is broken'}</option><option value="integration">{de ? 'Integration' : 'Integration'}</option></Select><div><label className="mb-1 block text-sm font-medium text-slate-700">{de ? 'Erzähl uns mehr' : 'Tell us more'}</label><textarea value={description} onChange={event => setDescription(event.target.value)} minLength={10} required disabled={!signedIn} className="min-h-32 w-full rounded-lg border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" /></div>{message && <p className="rounded-lg bg-blue-50 p-3 text-sm text-blue-900">{message}</p>}<Button type="submit" disabled={!signedIn || busy} className="w-full">{busy ? '…' : (de ? 'Vorschlag senden' : 'Send request')}</Button></form></div></section><section className="mt-12"><h2 className="text-2xl font-bold">{admin ? (de ? 'Admin-Inbox' : 'Admin inbox') : (de ? 'Deine Vorschläge' : 'Your requests')}</h2><div className="mt-4 space-y-3">{rows.map(row => <article key={row.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="font-bold">{row.title}</h3><p className="mt-1 text-sm text-slate-600">{row.description}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold capitalize">{row.status.replace('_', ' ')}</span></div>{row.admin_note && <p className="mt-3 rounded-lg bg-blue-50 p-3 text-sm text-blue-900">{row.admin_note}</p>}{admin && <Select className="mt-4 max-w-xs" value={row.status} onChange={event => void review(row.id, event.target.value)}>{['open','planned','in_progress','shipped','declined'].map(status => <option key={status} value={status}>{status.replace('_', ' ')}</option>)}</Select>}</article>)}{signedIn && !rows.length && <p className="rounded-2xl border border-dashed border-slate-300 p-8 text-center text-slate-500">{de ? 'Noch keine Vorschläge.' : 'No requests yet.'}</p>}</div></section></div></main>;
}
