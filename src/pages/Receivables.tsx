import{useEffect,useState}from'react';import{useApp}from'../contexts/AppContext';import{supabase}from'../lib/supabase';
export function Receivables(){const{workspaceId}=useApp();const[rows,setRows]=useState<any[]>([]);const[msg,setMsg]=useState('');useEffect(()=>{if(workspaceId)supabase.from('v_invoice_balances').select('*').eq('workspace_id',workspaceId).order('due_date').then(({data,error})=>error?setMsg(error.message):setRows(data||[]))},[workspaceId]);return <div><h1 className="text-3xl font-bold">Receivables</h1><p className="text-gray-600 mt-1 mb-6">Invoice balances and overdue amounts.</p>{msg&&<p>{msg}</p>}<div className="bg-white border rounded-xl overflow-hidden"><table className="w-full"><thead className="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th className="p-4">Invoice</th><th>Due</th><th>Total</th><th>Paid</th><th>Outstanding</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.invoice_id} className="border-t"><td className="p-4 font-medium">{r.document_number}</td><td>{r.due_date||'—'}</td><td>{r.currency} {Number(r.total).toFixed(2)}</td><td>{Number(r.amount_paid).toFixed(2)}</td><td className="font-semibold">{Number(r.amount_due).toFixed(2)}</td><td className={r.is_overdue?'text-red-600':''}>{r.is_overdue?'Overdue':'Open'}</td></tr>)}{!rows.length&&<tr><td colSpan={6} className="p-10 text-center text-gray-500">No open invoices.</td></tr>}</tbody></table></div></div>}
