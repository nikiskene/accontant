import{useEffect,useState}from'react';import{useApp}from'../contexts/AppContext';import{supabase}from'../lib/supabase';
export function Receivables(){const{workspaceId}=useApp();const[rows,setRows]=useState<any[]>([]);const[msg,setMsg]=useState('');const load=()=>{if(workspaceId)supabase.from('v_invoice_balances').select('*').eq('workspace_id',workspaceId).order('due_date').then(({data,error})=>error?setMsg(error.message):setRows(data||[]))};useEffect(load,[workspaceId]);
const pay=async(r:any)=>{const amount=Number(prompt(`Payment amount (maximum ${r.amount_due})`,String(r.amount_due)));if(!amount)return;const{error}=await supabase.rpc('record_customer_payment',{p_invoice_id:r.invoice_id,p_payment_date:new Date().toISOString().slice(0,10),p_amount:amount,p_reference:null});if(error)setMsg(error.message);else load()};
const remind=async(r:any)=>{if(!workspaceId)return;const{error}=await supabase.from('payment_reminders').insert({workspace_id:workspaceId,invoice_id:r.invoice_id,subject:`Payment reminder ${r.document_number}`,message:`Please arrange payment of ${r.currency} ${r.amount_due}.`});setMsg(error?error.message:'Draft reminder created. Sending stays disabled until Entra setup.')};
return <div><h1 className="text-3xl font-bold">Receivables</h1><p className="text-gray-600 mt-1 mb-6">Invoice balances, payments and reminders.</p>{msg&&<p className="bg-blue-50 p-3 rounded mb-4">{msg}</p>}<div className="bg-white border rounded-xl overflow-hidden"><table className="w-full"><thead className="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th className="p-4">Invoice</th><th>Due</th><th>Total</th><th>Paid</th><th>Outstanding</th><th>Status & actions</th></tr></thead><tbody>{rows.map(r=><tr key={r.invoice_id} className="border-t"><td className="p-4 font-medium">{r.document_number}</td><td>{r.due_date||'—'}</td><td>{r.currency} {Number(r.total).toFixed(2)}</td><td>{Number(r.amount_paid).toFixed(2)}</td><td className="font-semibold">{Number(r.amount_due).toFixed(2)}</td><td className={r.is_overdue?'text-red-600':''}>{r.is_overdue?'Overdue':'Open'}<div className="flex gap-2"><button className="text-xs text-blue-600" onClick={()=>pay(r)}>Record payment</button>{r.is_overdue&&<button className="text-xs text-blue-600" onClick={()=>remind(r)}>Draft reminder</button>}</div></td></tr>)}{!rows.length&&<tr><td colSpan={6} className="p-10 text-center text-gray-500">No open invoices.</td></tr>}</tbody></table></div></div>}
